app = "mythos-slack"
primary_region = "nrt"

[build]

[deploy]
  strategy = "immediate"

[[restart]]
  policy = "always"
  max_retries = 10

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
